import { describe, expect, it } from 'vitest';
import numberSeparate from '.';
describe('numberSeparate', () => {
  it('should be defined', () => {
    expect(numberSeparate).toBeDefined();
  });
});
