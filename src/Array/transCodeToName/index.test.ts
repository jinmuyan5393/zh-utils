import { describe, expect, it } from 'vitest';
import transCodeToName from '.';
describe('transCodeToName', () => {
  it('should be defined', () => {
    expect(transCodeToName).toBeDefined();
  });
});
