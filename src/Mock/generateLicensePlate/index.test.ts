import { describe, expect, it } from 'vitest';
import generateLicensePlate from '.';
describe('generateLicensePlate', () => {
  it('should be defined', () => {
    expect(generateLicensePlate).toBeDefined();
  });
});
