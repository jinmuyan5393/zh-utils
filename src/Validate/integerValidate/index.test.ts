import { describe, expect, it } from 'vitest';
import integerValidate from '.';
describe('integerValidate', () => {
  it('should be defined', () => {
    expect(integerValidate).toBeDefined();
  });
});
