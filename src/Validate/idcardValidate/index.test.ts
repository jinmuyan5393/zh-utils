import { describe, expect, it } from 'vitest';
import idcardValidate from '.';
describe('idcardValidate', () => {
  it('should be defined', () => {
    expect(idcardValidate).toBeDefined();
  });
});
