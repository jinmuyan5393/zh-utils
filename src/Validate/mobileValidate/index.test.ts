import { describe, expect, it } from 'vitest';
import mobileValidate from '.';
describe('mobileValidate', () => {
  it('should be defined', () => {
    expect(mobileValidate).toBeDefined();
  });
});
