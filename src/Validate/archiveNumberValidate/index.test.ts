import { describe, expect, it } from 'vitest';
import archiveNumberValidate from '.';
describe('archiveNumberValidate', () => {
  it('should be defined', () => {
    expect(archiveNumberValidate).toBeDefined();
  });
});
