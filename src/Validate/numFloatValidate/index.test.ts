import { describe, expect, it } from 'vitest';
import numFloatValidate from '.';
describe('numFloatValidate', () => {
  it('should be defined', () => {
    expect(numFloatValidate).toBeDefined();
  });
});
