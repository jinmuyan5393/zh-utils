import { describe, expect, it } from 'vitest';
import numFloat2Validate from '.';
describe('numFloat2Validate', () => {
  it('should be defined', () => {
    expect(numFloat2Validate).toBeDefined();
  });
});
