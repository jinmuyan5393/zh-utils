import { describe, expect, it } from 'vitest';
import licensePlateValidate from '.';
describe('licensePlateValidate', () => {
  it('should be defined', () => {
    expect(licensePlateValidate).toBeDefined();
  });
});
