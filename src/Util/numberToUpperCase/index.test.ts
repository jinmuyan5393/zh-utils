import { describe, expect, it } from 'vitest';
import numberToUpperCase from '.';
describe('numberToUpperCase', () => {
  it('should be defined', () => {
    expect(numberToUpperCase).toBeDefined();
  });
});
